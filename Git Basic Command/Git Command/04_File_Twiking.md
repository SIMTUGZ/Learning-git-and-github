# Git File Tweaking Commands

These commands help you delete, restore, and rename files in a Git repository.

1. Delete a tracked file from the repository and working directory:

        git rm "<file name>"

2. Restore a deleted file from the working directory:

        git restore "<file name>"

3. Rename or move a file:

        git mv "old file name" "new file name"
